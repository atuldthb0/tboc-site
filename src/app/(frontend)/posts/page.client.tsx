'use client'
import React from 'react'

const PageClient: React.FC = () => {
  // No header theme override - let it use the default based on site theme
  return <React.Fragment />
}

export default PageClient
